{
  "name": "dhis2-ai-extension",
  "version": "2.9.0",
  "private": true,
  "description": "Dependency-free validation and tests for the DHIS2 AI Assistant Chrome extension.",
  "scripts": {
    "check": "node scripts/validate.js",
    "test": "node --test tests/*.test.js"
  }
}
