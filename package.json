{
  "name": "dhis2-ai-assistant",
  "version": "2.8.13",
  "private": true,
  "description": "DHIS2 AI Assistant — Chrome MV3 extension. No build step: the background service worker loads focused ES-free modules under src/ via importScripts(). See ARCHITECTURE.md.",
  "scripts": {
    "verify": "node scripts/verify.js",
    "check": "node scripts/verify.js"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT"
}
